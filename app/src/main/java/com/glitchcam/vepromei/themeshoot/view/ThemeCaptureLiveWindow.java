package com.glitchcam.vepromei.themeshoot.view;

import android.content.Context;
import android.util.AttributeSet;

import com.meicam.sdk.NvsLiveWindow;

public class ThemeCaptureLiveWindow extends NvsLiveWindow {
    public ThemeCaptureLiveWindow(Context context) {
        super(context);
    }

    public ThemeCaptureLiveWindow(Context context, AttributeSet attributeSet) {
        super(context, attributeSet);
    }

    public ThemeCaptureLiveWindow(Context context, AttributeSet attributeSet, int i) {
        super(context, attributeSet, i);
    }
}
